<base:WpfUiWindow
    x:Class="Bloxstrap.UI.Elements.Bootstrapper.AstralstrapDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:Bloxstrap.UI.Elements.Base"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:Bloxstrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Width="500"
    Height="150"
    MinHeight="0"
    Background="Transparent"
    Closing="UiWindow_Closing"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="NoResize"
    WindowBackdropType="{Binding Path=WindowBackdropType, Mode=OneTime}"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">

    <Window.TaskbarItemInfo>
        <TaskbarItemInfo ProgressState="{Binding Path=TaskbarProgressState}" ProgressValue="{Binding Path=TaskbarProgressValue}" />
    </Window.TaskbarItemInfo>

    <Border
        Margin="6"
        Background="#F011131E"
        BorderBrush="#334A6E"
        BorderThickness="1"
        CornerRadius="14">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.65" ShadowDepth="4" Color="#000000" />
        </Border.Effect>

        <Grid Margin="18,14,18,14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Hidden drag titlebar -->
            <ui:TitleBar Grid.Row="0" Height="0" CanMaximize="False" ShowClose="False" ShowMaximize="False" ShowMinimize="False" Visibility="Collapsed" />

            <!-- Main Content Area -->
            <Grid Grid.Row="1" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Glowing Icon Frame -->
                <Border
                    Grid.Column="0"
                    Width="52"
                    Height="52"
                    Margin="0,0,16,0"
                    Background="#1E2235"
                    BorderBrush="#3D4566"
                    BorderThickness="1"
                    CornerRadius="12"
                    VerticalAlignment="Center">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="12" Opacity="0.4" ShadowDepth="0" Color="#00E5FF" />
                    </Border.Effect>
                    <Image
                        Width="38"
                        Height="38"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        RenderOptions.BitmapScalingMode="HighQuality"
                        Source="{Binding Icon, Mode=OneWay}" />
                </Border>

                <!-- Text Info -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,12,0">
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            FontSize="15"
                            FontWeight="Bold"
                            Foreground="#00E5FF"
                            Text="Astralstrap" />
                        <TextBlock
                            Grid.Column="1"
                            Margin="8,1,0,0"
                            FontSize="11"
                            Foreground="#6B7280"
                            Text="v2.0.0" />
                    </Grid>
                    <TextBlock
                        FontSize="13"
                        FontWeight="Normal"
                        Foreground="#D1D5DB"
                        Text="{Binding Message, Mode=OneWay}"
                        TextTrimming="CharacterEllipsis"
                        TextWrapping="NoWrap" />
                </StackPanel>

                <!-- Cancel Button -->
                <ui:Button
                    Grid.Column="2"
                    Height="32"
                    Padding="14,0"
                    VerticalAlignment="Center"
                    Appearance="Secondary"
                    Command="{Binding CancelInstallCommand}"
                    Content="{x:Static resources:Strings.Common_Cancel}"
                    IsEnabled="{Binding CancelEnabled}"
                    Visibility="{Binding CancelButtonVisibility}" />
            </Grid>

            <!-- Modern Progress Bar -->
            <Grid Grid.Row="2" Margin="0,10,0,2">
                <ProgressBar
                    Height="5"
                    BorderThickness="0"
                    Foreground="#00E5FF"
                    Background="#1E2438"
                    IsIndeterminate="{Binding ProgressIndeterminate, Mode=OneWay}"
                    Maximum="{Binding ProgressMaximum, Mode=OneWay}"
                    Value="{Binding ProgressValue, Mode=OneWay}" />
            </Grid>
        </Grid>
    </Border>
</base:WpfUiWindow>
