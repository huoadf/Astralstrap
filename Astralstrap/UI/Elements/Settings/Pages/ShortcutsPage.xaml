<ui:UiPage
    x:Class="Bloxstrap.UI.Elements.Settings.Pages.ShortcutsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Bloxstrap.UI.Elements.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dmodels="clr-namespace:Bloxstrap.UI.ViewModels.Settings"
    xmlns:local="clr-namespace:Bloxstrap.UI.Elements.Settings.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:Bloxstrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="ShortcutsPage"
    d:DataContext="{d:DesignInstance dmodels:ShortcutsViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="1000"
    d:DesignWidth="800"
    Scrollable="True"
    mc:Ignorable="d">

    <StackPanel Margin="0,0,14,14">
        <TextBlock
            Margin="0,0,0,16"
            FontSize="14"
            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
            Text="{x:Static resources:Strings.Menu_Shortcuts_Description}" />

        <controls:OptionControl
            Description="{Binding Source={x:Static resources:Strings.Menu_Shortcuts_ExtractIcons_Description}, Converter={StaticResource StringFormatConverter}, ConverterParameter='https://github.com/bloxstraplabs/bloxstrap/wiki/Using-Roblox-icons-for-your-shortcuts'}"
            Header="{x:Static resources:Strings.Menu_Shortcuts_ExtractIcons_Title}"
            HelpLink="https://github.com/bloxstraplabs/bloxstrap/wiki/Using-Roblox-icons-for-your-shortcuts">
            <ui:ToggleSwitch IsChecked="{Binding ExtractIconsTask.NewState, Mode=TwoWay}" />
        </controls:OptionControl>

        <Separator Margin="0,6,0,4" />

        <TextBlock
            FontSize="20"
            FontWeight="Bold"
            Text="{x:Static resources:Strings.Menu_Shortcuts_General_Title}" />
        <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static resources:Strings.Menu_Shortcuts_General_Description}" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <controls:OptionControl
                Grid.Column="0"
                Margin="0,0,4,0"
                Header="{x:Static resources:Strings.Common_Shortcuts_Desktop}">
                <ui:ToggleSwitch IsChecked="{Binding DesktopIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>

            <controls:OptionControl
                Grid.Column="1"
                Margin="4,0,0,0"
                Header="{x:Static resources:Strings.Common_Shortcuts_StartMenu}">
                <ui:ToggleSwitch IsChecked="{Binding StartMenuIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>
        </Grid>

        <Separator Margin="0,8,0,4" />

        <TextBlock
            FontSize="20"
            FontWeight="Bold"
            Text="{x:Static resources:Strings.Menu_Shortcuts_Function_Title}" />
        <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{x:Static resources:Strings.Menu_Shortcuts_Function_Description}" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <controls:OptionControl
                Grid.Column="0"
                Margin="0,0,2,0"
                Header="{x:Static resources:Strings.LaunchMenu_LaunchRoblox}">
                <ui:ToggleSwitch IsChecked="{Binding PlayerIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>

            <controls:OptionControl
                Grid.Column="1"
                Margin="2,0,2,0"
                Header="{x:Static resources:Strings.LaunchMenu_LaunchRobloxStudio}">
                <ui:ToggleSwitch IsChecked="{Binding StudioIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>

            <controls:OptionControl
                Grid.Column="2"
                Margin="2,0,0,0"
                Header="{x:Static resources:Strings.Menu_Title}">
                <ui:ToggleSwitch IsChecked="{Binding SettingsIconTask.NewState, Mode=TwoWay}" />
            </controls:OptionControl>
        </Grid>

        <Separator Margin="0,8,0,4" />

        <TextBlock
            FontSize="20"
            FontWeight="Bold"
            Text="{x:Static resources:Strings.Common_Miscellaneous}" />

        <ui:CardExpander Margin="0,8,0,0" IsExpanded="True">
            <ui:CardExpander.Header>
                <StackPanel>
                    <TextBlock FontSize="14" Text="{x:Static resources:Strings.Menu_Shortcut_GameShortcut_Title}" />
                    <TextBlock FontSize="12" Text="{x:Static resources:Strings.Menu_Shortcut_GameShortcut_Description}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                </StackPanel>
            </ui:CardExpander.Header>

            <StackPanel>
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Width="100" Height="100" CornerRadius="12" ClipToBounds="True" Background="{DynamicResource ControlAltFillColorQuarternaryBrush}">
                        <Image Source="{Binding PreviewIconUrl}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Border>

                    <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding PreviewName}" FontSize="20" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <ui:SymbolIcon Symbol="Tag24" FontSize="14" Margin="0,0,6,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{Binding PreviewId}" FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </StackPanel>
                </Grid>

                <StackPanel>
                    <Label Content="Search for a Game" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <ComboBox Height="40" 
                      IsEditable="True" 
                      StaysOpenOnEdit="True" 
                      ItemsSource="{Binding SearchResults}"
                      SelectedItem="{Binding SelectedSearchResult}"
                      Text="{Binding SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      TextSearch.TextPath="Name"
                      SelectedValuePath="RootPlaceId">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,4,8,4" Height="40">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Width="40" Height="40">
                                        <Rectangle RadiusX="8" RadiusY="8">
                                            <Rectangle.Fill>
                                                <ImageBrush ImageSource="{Binding ThumbnailUrl}" Stretch="UniformToFill" />
                                            </Rectangle.Fill>
                                        </Rectangle>
                                    </Border>
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" Margin="12,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                        <ui:SymbolIcon Symbol="People24" Margin="0,0,4,0" FontSize="16"/>
                                        <TextBlock Text="{Binding PlayerCount}"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <Label Content="Server Instance ID" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        <ui:TextBox Text="{Binding JobId, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0" Icon="DocumentSearch24" PlaceholderText="GUID/JobID" />
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <Label Content="Private Access Code" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        <ui:TextBox Text="{Binding AccessCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0" Icon="LockClosed24" PlaceholderText="Access Code" />
                    </StackPanel>
                </Grid>

                <Separator Margin="0,8"/>

                <DockPanel>
                    <ui:Button Command="{Binding CreateGameShortcutCommand}" Appearance="Primary" Content="Create Shortcut" Icon="Add24" />
                    <TextBlock Text="{Binding ShortcutStatus}" Margin="16,8,0,0" FontSize="12" />
                </DockPanel>
            </StackPanel>
        </ui:CardExpander>
    </StackPanel>
</ui:UiPage>
