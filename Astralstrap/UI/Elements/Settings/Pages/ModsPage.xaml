<ui:UiPage
    x:Class="Bloxstrap.UI.Elements.Settings.Pages.ModsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:models="clr-namespace:Bloxstrap.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dmodels="clr-namespace:Bloxstrap.UI.ViewModels.Settings"
    xmlns:vmodels="clr-namespace:Bloxstrap.UI.ViewModels"
    xmlns:controls="clr-namespace:Bloxstrap.UI.Elements.Controls"
    xmlns:resources="clr-namespace:Bloxstrap.Resources"
    Title="ModsPage" 
    Scrollable="True" 
    AllowDrop="True" 
    Drop="Page_Drop"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance dmodels:ModsViewModel}"
    Loaded="Page_Loaded">

    <StackPanel Margin="0,0,14,14">
        <TextBlock
            FontSize="14"
            Foreground="{StaticResource TextFillColorSecondaryBrush}"
            Text="Manage your mods and their application order. Mods at the bottom have higher priority and will overwrite mods above them."
            TextWrapping="Wrap" />

        <Grid Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ui:CardAction Grid.Column="0" Margin="0,0,3,0" Command="{Binding OpenCommunityModsCommand}" Icon="BuildingShop20">
                <StackPanel>
                    <TextBlock FontSize="14" Text="Community" />
                    <TextBlock Margin="0,2,0,0" Padding="0,0,8,0" FontSize="11" Foreground="{StaticResource TextFillColorTertiaryBrush}" Text="Explore mods." TextWrapping="Wrap" />
                </StackPanel>
            </ui:CardAction>

            <ui:CardAction Grid.Column="1" Margin="2,0,2,0" Command="{Binding OpenPresetModsCommand}" Icon="ReadingModeMobile24">
                <StackPanel>
                    <TextBlock FontSize="14" Text="Presets" TextWrapping="Wrap" />
                    <TextBlock Margin="0,2,0,0" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Built-in mods." TextWrapping="Wrap" />
                </StackPanel>
            </ui:CardAction>

            <ui:CardAction Grid.Column="2" Margin="2,0,2,0" Command="{Binding OpenModGeneratorCommand}" Icon="Color20">
                <StackPanel>
                    <TextBlock FontSize="14" Text="Generator" TextWrapping="Wrap" />
                    <TextBlock Margin="0,2,0,0" FontSize="11" Foreground="{StaticResource TextFillColorTertiaryBrush}" Text="Create mods." TextWrapping="Wrap" />
                </StackPanel>
            </ui:CardAction>

            <ui:CardAction Grid.Column="3" Margin="3,0,0,0" Command="{Binding OpenCustomSoundsCommand}" Icon="Speaker224">
                <StackPanel>
                    <TextBlock FontSize="14" Text="Sounds" TextWrapping="Wrap" />
                    <TextBlock Margin="0,2,0,0" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Death &amp; Audio." TextWrapping="Wrap" />
                </StackPanel>
            </ui:CardAction>
        </Grid>

        <Separator Margin="0,8" />

        <ui:InfoBar Severity="Informational" Message="You can drag and drop your mod to auto add it here" Padding="8" IsOpen="True" Title="Important" IsClosable="False" Background="{DynamicResource PrimaryBackgroundColor}" />

        <Grid Margin="0,8,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:Button Grid.Column="0"
               Margin="0,0,3,0"
               Icon="Add24"
               Content="Add new mod"
               HorizontalAlignment="Stretch"
               Appearance="Secondary"
               Command="{Binding AddModCommand}" />
            <ui:Button Grid.Column="1"
               Margin="2,0,2,0"
               Icon="Warning24"
               Content="Check Conflicts"
               HorizontalAlignment="Stretch"
               Appearance="Secondary"
               Command="{Binding CheckConflictsCommand}" />
            <ui:Button Grid.Column="2"
               Margin="3,0,0,0"
               Icon="Image24"
               Content="Preview Assets"
               HorizontalAlignment="Stretch"
               Appearance="Secondary"
               Command="{Binding OpenModPreviewCommand}" />
        </Grid>

        <ItemsControl ItemsSource="{Binding Modifications, Mode=OneWay}">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type models:ModConfig}">
                    <ui:Card Margin="0,0,0,8" >
                        <Grid Margin="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <ui:Button Grid.Column="0" 
                                       Icon="Delete24" 
                                       Appearance="Danger"
                                       Height="35"
                                       Command="{Binding DataContext.DeleteModCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                       CommandParameter="{Binding}" />

                            <ui:TextBox Grid.Column="1" Width="300"
                                        Text="{Binding FolderName, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" 
                                        PlaceholderText="Mod Name"
                                        Margin="8,0" 
                                        GotFocus="ModName_GotFocus"
                                        LostFocus="ModName_LostFocus" />

                            <ComboBox Grid.Column="3" Margin="0,0,8,0"
                                      SelectedValue="{Binding Target, Mode=TwoWay}" 
                                      SelectedValuePath="Content"
                                      Width="150">
                                <ComboBoxItem Content="Both" />
                                <ComboBoxItem Content="Player" />
                                <ComboBoxItem Content="Studio" />
                                <ComboBoxItem Content="Disabled" />
                            </ComboBox>

                            <ui:Button Grid.Column="4" 
                                       Icon="Folder24" 
                                       Height="35"
                                       Command="{Binding DataContext.OpenModFolderCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                       CommandParameter="{Binding}" />

                            <ui:Button Grid.Column="5" Margin="4,0,0,0"
                                       Icon="ArrowUp24" 
                                       Height="35"
                                       Command="{Binding DataContext.MoveUpCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                       CommandParameter="{Binding}" />

                            <ui:Button Grid.Column="6" Margin="4,0,0,0"
                                       Icon="ArrowDown24" 
                                       Height="35"
                                       Command="{Binding DataContext.MoveDownCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                       CommandParameter="{Binding}" />
                        </Grid>
                    </ui:Card>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</ui:UiPage>
